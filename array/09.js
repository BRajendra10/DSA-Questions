// 9. Write a program to check whether a variable is an array.

const array = [1, 2, 3, 4, 5];
console.log(Array.isArray(array));

// Alternative: Using instanceof operator
console.log(array instanceof Array);