// 30. Write a program to clone an array using spread operator.

const original = ["apple", "banana", "cherry"];

const clone = [...original];

console.log("Original:", original);
console.log("Clone:", clone);